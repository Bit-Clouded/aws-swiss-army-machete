---
platform: linux

params:
  REPOSITORY: ((repository))
  #TAG: latest <- overriden using script
  CONTEXT: github-machete-dockerfile

inputs:
- name: container-semver
- name: cloudformation-artifact
- name: s3-artifact
- name: github-machete-dockerfile
- name: golang-cli

outputs:
- name: image

caches:
- path: cache

run: {path: /github-machete-dockerfile/docker/build}